#include<stdio.h>

/// @brief 
/// @return 
int main(){ 
    // int length = 3;
    // int breadth = 5;
    int length;
    int breadth;
    printf("enter length:");
    scanf("%d",&length);

    printf("enter breadth:");
    scanf("%d",&breadth);
    
    printf("The area of rectangle is %d",length*breadth);
    return 0;
}